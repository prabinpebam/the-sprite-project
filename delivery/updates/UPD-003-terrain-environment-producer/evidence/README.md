# UPD-003 Evidence

Evidence is stored under `runs/<RUN-ID>/`.

A promotable run must include:

- all eight terrain task flows through production web or packaged Electron UI on declared hosts;
- raw accessibility/control/canvas capture, screenshots, console/page/network events, downloads, and persistence state;
- exact 64 material/mask pixel fixtures and deterministic repeated atlas hashes;
- IndexedDB v4 migration, 100-edit performance, conflict, snapshot, copy, deletion, and interruption evidence;
- generic and Godot terrain package inspection plus pinned Godot validation;
- archive-v3, old-reader rejection, web→Electron→web parity, offline restart, and rollback preservation;
- every affected MVP, UPD-001, and UPD-002 regression;
- zero unexplained severity 0–2 issue or anomaly.

Statuses remain approved until generated evidence owns implementation promotion.
